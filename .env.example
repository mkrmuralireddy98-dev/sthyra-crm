# Copy to `.env` and customize.

# Postgres connection string used by services/org-service and others.
DATABASE_URL=postgres://plumb:plumb@localhost:5432/plumb

# HTTP server bind.
PORT=8080
HOST=0.0.0.0

# Future: identity, S3, queue URLs go here.
# OIDC_ISSUER_URL=
# AWS_REGION=
# AWS_S3_BUCKET=
